<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tracker.income.mapper.IncomeMapper">

    <select id="getList" resultType="org.tracker.income.domain.IncomeDTO">
        <![CDATA[
        SELECT * FROM INCOME WHERE id > 0 ORDER BY id DESC
        ]]>
    </select>

    <insert id="insert">
        <selectKey keyProperty="id" order="BEFORE" resultType="long">
            SELECT seq_income.nextval FROM dual
        </selectKey>

        INSERT INTO INCOME (ID, DESCRIPTION, AMOUNT, INCOME_DATE, CATEGORY, PAYMENT_METHOD, NOTE)
        VALUES (seq_income.nextval, #{description}, #{amount}, #{incomeDate}, #{category}, #{paymentMethod}, #{note})
    </insert>

    <select id="read" resultType="org.tracker.income.domain.IncomeDTO">
        <![CDATA[
        SELECT * FROM INCOME WHERE id =#{id}
        ]]>
    </select>

    <update id="update">
        UPDATE INCOME
        SET DESCRIPTION = #{description},
            AMOUNT = #{amount},
            INCOME_DATE = #{incomeDate},
            CATEGORY = #{category},
            PAYMENT_METHOD = #{paymentMethod},
            NOTE = #{note}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM INCOME WHERE id =#{id}
    </delete>

</mapper>

